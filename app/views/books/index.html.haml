%h1 Books

%h2= button_to "Import from Kindle.", imports_path

%ul.books
  - for book in @books
    %li
      = link_to book.title, book
      (#{pluralize(book.clippings.size, "Clipping")}, #{pluralize(book.notes.size, "Note")})